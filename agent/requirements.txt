fastapi>=0.115.0
uvicorn[standard]>=0.30.6
httpx>=0.27.2
pydantic>=2.10
rich>=13.8.1
python-dotenv>=1.0.1
anyio>=4.6.0

# Real on-chain payments (Arc testnet)
eth-account>=0.13.7
web3>=7.16.0

# Real LangChain tool-calling orchestrator (needs GOOGLE_API_KEY, ANTHROPIC_API_KEY, or OPENAI_API_KEY)
langchain>=1.3.11
langchain-google-genai>=2.0.0  # free tier, aistudio.google.com/apikey -- checked first
langchain-anthropic>=1.4.8
# langchain-openai>=1.3.3  # uncomment if using OPENAI_API_KEY instead

# Installed for the documented upgrade path (see payments/x402.py docstring)
# -- not imported by the current on-chain-transfer-based x402 flow.
x402[fastapi,httpx,evm]>=2.14.0
circle-developer-controlled-wallets>=9.6.0
